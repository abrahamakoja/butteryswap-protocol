# correct que spelling

# remove audit files from github upload,updat .env to reflect this

# cache storage variables withion loops and also use unchecked approach.. --(.lenght)

# aany time state change emit event

# also use pull over push for users to withdraw funds

# remove fallbacks from contract dat do not need it

# alsways keep a mapping of user balances when working with erc20 and native tokens

# integrate the pull over push design for contract with funds.

# use bound for deposit and withdrawals

# \_amount = bound(\_amount,0,YeildERC20.balanceOf(user))

# handle msg.value from external functions and pass its value to the internal

# use chainlink for every block.timestamp integration

# add a revertIfZero modifier to handle zero inputs

# implement FREI-PIE instead of CEI

# add slippage protection

# add comments for slither and aderyn in relevant areas

# do low level calls on relevant areas

# make erc20 dynamic especially for unit and fuzz test

# use contract conning for the loan requests

# add admin manager contract

# add a fee contract

# config contract

# remove all console logs before production

# ensure all enums have default state

# add env.example

# use block.number instead of timestamp

# for best practices and naming conventions
